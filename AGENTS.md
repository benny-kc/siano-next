# AGENTS

The canonical guide for this repo is **[CLAUDE.md](CLAUDE.md)** — read it first.
It covers the local-first architecture, how to run and test (no build step,
`node --test`), the operation set and merge rules, domain rules (integer cents,
per-budget balances, never-clamp locked shares), the file map, the security
hardening and env vars, logging, gotchas, and the roadmap.

Deeper design docs: [docs/architecture.md](docs/architecture.md) and
[docs/security.md](docs/security.md).
