// Portuguese (Português). Translations pending — an empty catalog falls back to
// English. To ship Portuguese, add the keys from en.js (JS-rendered strings) and
// the data-i18n keys from index.html (static chrome), keeping any {placeholder}
// tokens and HTML tags intact.
import { registerVersion } from "../version.js";
registerVersion("js/i18n/pt.js", 1);

export default {};
