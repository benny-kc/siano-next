// Japanese (日本語). Translations pending — an empty catalog falls back to
// English. To ship Japanese, add the keys from en.js (JS-rendered strings) and
// the data-i18n keys from index.html (static chrome), keeping any {placeholder}
// tokens and HTML tags intact.
import { registerVersion } from "../version.js";
registerVersion("js/i18n/ja.js", 1);

export default {};
